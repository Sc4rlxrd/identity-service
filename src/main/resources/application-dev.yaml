spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/auth_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

    hikari:
      pool-name: IdentityService-HikariPool
      minimum-idle: 1
      maximum-pool-size: 5
      connection-timeout: 30000
      validation-timeout: 5000
      idle-timeout: 600000
      max-lifetime: 1800000

  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 5s
      timeout: 5s

api:
  security:
    token:
      secret: dd2e2add61a0cce0eb2bfb9c2d812515

app:
  admin:
    email: SCARLXRD@teste.com
    password: 1234569999

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

monitoring:
  prometheus:
    username: prometheus
    password: prometheus

